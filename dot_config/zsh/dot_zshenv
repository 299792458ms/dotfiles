export ZDOTDIR="$HOME"/.config/zsh # put this in /etc/zsh/zshenv or /etc/zshenv

export PATH="$PATH:"$HOME"/.cargo/bin"
export PATH="$PATH:"$HOME"/.local/bin"
export XDG_DATA_HOME="$HOME"/.local/share
export XDG_CONFIG_HOME="$HOME"/.config
export XDG_STATE_HOME="$HOME"/.local/state
export XDG_CACHE_HOME="$HOME"/.cache
export GOPATH="$XDG_DATA_HOME"/go
export GTK2_RC_FILES="$XDG_CONFIG_HOME"/gtk-2.0/gtkrc
export STARSHIP_CONFIG="$HOME"/.config/starship/starship.toml
export HISTFILE="$XDG_STATE_HOME"/zsh/history

export SUDO_EDITOR='/usr/bin/helix'
export EDITOR='helix'
export VISUAL='helix'
export BROWSER='librewolf'
export TERMINAL='foot'

export ELECTRON_OZONE_PLATFORM_HINT='wayland'
export XDG_CURRENT_DESKTOP='river'
export LIBSEAT_BACKEND='logind'

export FZF_CTRL_R_OPTS="
  --height 100%
  --bind 'ctrl-y:execute-silent(echo -n {2..} | wl-copy)+abort'
  --color=16,header:02,current-bg:-1,current-hl:underline:01,hl:03,current-fg:-1,scrollbar:04,prompt:05,pointer:01,info:04
  --header 'CTRL-Y: copy, ctrl-/: toggle wrap'
  --reverse"

export FZF_ALT_C_OPTS="
  --walker-skip .git,node_modules,target
  --preview 'tree -CL 2 {}'"
